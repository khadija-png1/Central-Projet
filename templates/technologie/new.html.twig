    <h2 class="modal-title text-info w-50 text-center mb-4">Ajouter une technologie</h2>
      {{ form_start(form, {'action': path('app_technologie_new'), 'method': 'POST'}) }}
          {{ form_row(form.nom) }}
          {{ form_row(form.version) }}
        <div class="modal-footer ">
            {{ form_widget(form.Enregistrer, {'attr': {'class': 'btn btn-info'}}) }}
        </div>
      {{ form_end(form) }}

