<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Central Projet</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

  <style>
    /* Hero Section */
    .hero {
      background-color: #0d6efd;
      padding: 100px 0;
      text-align: center;
      animation: fadeIn 2s ease-in-out;
    }
    .hero h1, .hero p {
      color: white;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(-20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* Feature icons */
    .feature-icon {
      font-size: 3rem;
      color: #0d6efd;
    }

    /* Footer */
    footer {
      background-color: #0d6efd;
      color: white;
      padding: 20px 0;
      text-align: center;
    }

    /* SVG logo styling inside navbar */
    .navbar-logo svg {
      height: 60px;
      width: auto;
      display: block;
    }
    /* Frame and text styles inside SVG are already inline */
  </style>
</head>
<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm fixed-top">
    <div class="container d-flex align-items-center">
      <a class="navbar-brand navbar-logo" href="#" aria-label="Logo Central Projet">
        <svg width="180" height="60" viewBox="0 0 340 120" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Logo Central Projet" >
          <style>
            .frame {
              fill: none;
              stroke: #0d6efd;
              stroke-width: 5;
              rx: 10;
              ry: 10;
              transform-origin: center;
              transform-box: fill-box;
              transform: rotate(-5deg);
            }
            .letter {
              font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
              font-weight: 900;
              font-size: 70px;
              fill: #212529;
            }
            .text-primary {
              font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
              font-weight: 700;
              font-size: 48px;
              fill: #0d6efd;
            }
          </style>

          <!-- Encadre C -->
          <rect x="5" y="10" width="70" height="100" class="frame"/>
          <text x="15" y="85" class="letter">C</text>

          <!-- "entral" -->
          <text x="65" y="85" class="text-primary">entral</text>

          <!-- Encadre P décalé à gauche -->
          <rect x="180" y="10" width="70" height="100" class="frame"/>
          <text x="195" y="85" class="letter">P</text>

          <!-- "rojet" décalé un peu plus à gauche -->
          <text x="240" y="85" class="text-primary">rojet</text>
        </svg>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMenu" aria-controls="navMenu" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navMenu">
        <ul class="navbar-nav ms-auto me-3">
          <li class="nav-item"><a class="nav-link text-dark" href="#">Accueil</a></li>
          <li class="nav-item"><a class="nav-link text-dark" href="#about">À propos</a></li>
          <li class="nav-item"><a class="nav-link text-dark" href="#features">Fonctionnalités</a></li>
        </ul>
        <div class="d-flex">
          <a href="{{ path('app_login') }}" class="btn btn-outline-primary me-2">Connexion</a>
          <a href="{{ path('app_register') }}" class="btn btn-primary text-white">Inscription</a>
        </div>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <section class="hero mt-5 pt-5">
    <div class="container">
      <h1 class="display-4 fw-bold">Bienvenue sur Central Projet</h1>
      <p class="lead">Votre plateforme centralisée pour la gestion efficace des projets.</p>
      <div class="mt-4">
        <a href="{{ path('app_login') }}" class="btn btn-outline-light me-2">Connexion</a>
        <a href="{{ path('app_register') }}" class="btn  btn-outline-light me-2">Inscription</a>
      </div>
    </div>
  </section>

  <!-- À propos -->
  <section id="about" class="py-5 text-center bg-white">
    <div class="container">
      <h2 class="text-primary mb-4">À propos de nous</h2>
      <p class="lead">Central Projet est une solution moderne pour gérer vos projets, suivre les tâches et collaborer efficacement avec votre équipe.</p>
    </div>
  </section>

  <!-- Fonctionnalités -->
  <section id="features" class="py-5 bg-light">
    <div class="container">
      <h2 class="text-primary mb-5 text-center">Fonctionnalités clés</h2>
      <div class="row g-4">
        <div class="col-md-4 text-center">
          <div class="feature-icon mb-3">🔔</div>
          <h5>Système de Notification</h5>
          <p>Recevez des alertes automatiques avant les dates d’échéance d’hébergements et d'autres éléments critiques.</p>
        </div>
        <div class="col-md-4 text-center">
          <div class="feature-icon mb-3">🧑‍💻</div>
          <h5>Attribution des Projets</h5>
          <p>Assignez facilement les projets aux développeurs et suivez leur charge de travail en temps réel.</p>
        </div>
        <div class="col-md-4 text-center">
          <div class="feature-icon mb-3">📊</div>
          <h5>Suivi des Statuts</h5>
          <p>Surveillez l'avancement des projets par statut (en cours, terminé, en attente) depuis une interface claire pour l’administrateur.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer>
    <div class="container">
      <p>&copy; 2025 Central Projet. Tous droits réservés.</p>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
