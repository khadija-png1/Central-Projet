<table class="table table-striped table-bordered">
    <tbody>
        <tr> 
            <th>Projets</th> 
            <td>    
                {% for projet in developpeur.projets %}
                    {{ projet.nom }}
                    <br>
                    <br>
                        {% else %}
                    Aucun projet associé.
                {% endfor %}
            </td>
        </tr>
    </tbody>
</table>
