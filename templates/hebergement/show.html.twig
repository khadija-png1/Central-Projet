<table class="table table-striped table-bordered">
    <tbody>
           <tr> 
                <th>Projets</th>
                <td>      
                    {% for projet in hebergement.projets %}
                        {{ projet.nom }}
                        <br>
                        <br>
                             {% else %}
                        <li>Aucun projet associé.</li>
                    {% endfor %}
                </td>
            </tr>     
    </tbody>
</table>
